@*@page "/customer/revalidateemail"*@
@layout _ColumnsOne

@code{

    private EmailRevalidationModel Model { get; set; }

}

@if (needStub) { }
else if (Model != null)
{
    //title
    Html.AddTitleParts(T("PageTitle.AccountActivation").Text);
    //page class
    Html.AppendPageCssClassParts("html-email-revalidation-page");

    <div class="page email-revalidation-page">
        <div class="page-title">
            <h1>@T("Account.EmailRevalidation")</h1>
        </div>
        <div class="page-body">
            <div class="result">
                @Model.Result
            </div>
        </div>
    </div>
}